"""User Order History.

Responsibilities:
- List user's own orders (recent first)
- Single order detail: items, total, status, payment method
- Track order status updates
- Re-order button (future)
"""

# TODO: Extract user-facing order views from src/handlers/orders.py

# async def orders_history_handler(update, context):
#     """Show user's order history list."""
#     pass

# async def order_detail_handler(update, context):
#     """Show single order detail for user."""
#     pass
