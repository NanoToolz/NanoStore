"""Admin Dashboard — live stats screen.

Responsibilities:
- Fetch and display live stats: users, orders, revenue, pending proofs, tickets, top-ups
- Edit message in place (never delete)
- Entry point for admin panel
"""

# TODO: Move admin_dashboard_handler() from src/handlers/admin.py here
# Source: src/handlers/admin.py -> admin_dashboard_handler()
