"""Admin Screen Content Manager.

Responsibilities:
- Per-screen image: set, clear, toggle all images ON/OFF
- Global image mode: one image for all screens vs per-screen
- Per-screen custom text/caption: set, clear (reset to default)
- Screens: welcome, shop, cart, orders, wallet, support, admin_panel
"""

# TODO: Move from src/handlers/admin_content.py and src/handlers/admin.py:
# admin_img_panel_handler, admin_img_set_handler, admin_img_clear_handler
# admin_img_toggle_handler, admin_global_img_toggle_handler
# admin_txt_set_handler, admin_txt_clear_handler
# admin_welcome_image_handler
